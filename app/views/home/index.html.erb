<div class="row">
  <div class="large-6 columns">
    <br/>
    <div class="panel callout radius text-center">
      <% unless @attendee.nil? %>
        <p>Welcome back, <%= @attendee.name %>!</p>
      <% end %>
      <%= link_to 'Create', new_room_path, class: 'round button' %>
      a room
      <% unless @attendee.nil? %>
        <br />
        Or rejoin <%= link_to @attendee.room.display_name, @attendee.room, class: 'round button' %>
      <% end %>
      <% unless @room.nil? %>
          <br />
          Or readminister <%= link_to @room.display_name, admin_room_path(@room), class: 'round button' %>
      <% end %>
    </div>
  </div>
</div>